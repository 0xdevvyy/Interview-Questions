**Problem**

Query all columns (attributes) for every row in the CITY table.

**Solution**

```sql
SELECT *
FROM CITY
```

i think just an answer is not enough, i think i need to bigkasin yung though process


first, FROM CITY, SQL will get all of the data first from the CITY and then sql will FILTER it from SELECT if all of the columns will be return or not.
